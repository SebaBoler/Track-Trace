
endpoint: http://localhost:4466/tracktrace/dev
# endpoint: ${env:AUTHORIZATION_SERVICE_ENDPOINT}
datamodel: datamodel.graphql
seed:
  import: seed-tracktrace.graphql
hooks:
  post-deploy:  
    graphql get-schema --project tracktrace-api